name: pycomlink-dev
channels:
  - conda-forge
dependencies:
 - numpy
 - scipy
 - pandas>=0.22
 - matplotlib
 - numba
 - h5py
 - xarray
 - netcdf4
 - shapely
 - pyproj
 - tqdm
 - pykrige
 - tensorflow
 - future
 - pip
 - pip:
   - git+https://github.com/pycomlink/pycomlink.git#egg=pycomlink
